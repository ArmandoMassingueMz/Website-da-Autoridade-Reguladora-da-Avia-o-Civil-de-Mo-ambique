<!-- Hero Section Contactos -->
<!-- Hero Section Contactos -->
<!-- Hero Section Contactos -->
<!-- Hero Section Contactos - COMPACTO -->
<section class="page-hero-section">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-6" data-aos="fade-right">
                <h1 class="hero-title mb-3">Contacte-nos</h1>
                <p class="hero-subtitle mb-4">Estamos aqui para ajudar. Entre em contacto connosco para qualquer dúvida, informação ou solicitação de serviços.</p>
                
                <!-- Informações de Contacto - VERSÃO COMPACTA -->
                <div class="contact-quick-info mb-3">
                    <div class="row g-2">
                        <div class="col-12">
                            <div class="d-flex align-items-center gap-2 py-1">
                                <i class="fas fa-phone text-warning" style="font-size: 0.9rem;"></i>
                                <span class="text-white" style="font-size: 0.9rem;">
                                    <strong>Telefone:</strong> (+258) 21468900
                                </span>
                            </div>
                        </div>
                        
                        <div class="col-12">
                            <div class="d-flex align-items-center gap-2 py-1">
                                <i class="fas fa-envelope text-warning" style="font-size: 0.9rem;"></i>
                                <span class="text-white" style="font-size: 0.9rem;">
                                    <strong>Email:</strong> geral@iacm.gov.mz
                                </span>
                            </div>
                        </div>
                        
                        <div class="col-12">
                            <div class="d-flex align-items-center gap-2 py-1">
                                <i class="fas fa-clock text-warning" style="font-size: 0.9rem;"></i>
                                <span class="text-white" style="font-size: 0.9rem;">
                                    <strong>Horário:</strong> 7:30–15:30 (Segunda a Sexta)
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Botão Compacto -->
                <div class="hero-buttons mt-3">
                    <button class="btn btn-hero btn-sm py-2 px-3" data-bs-toggle="modal" data-bs-target="#staffModal">
                        <i class="fas fa-users me-2"></i>Ver Colaboradores
                    </button>
                </div>
            </div>
            
            <div class="col-lg-6 text-center" data-aos="fade-left" data-aos-delay="200">
                <div class="hero-image">
                    <img src="/images/iacmlogopreview1.png" alt="Contactos IACM" class="img-fluid floating">
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Formulário de Contacto -->
<section class="contact-form-section py-5">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 mx-auto">
                <div class="contact-form-card card-modern">
                    <div class="card-body p-5">
                        <h3 class="text-center mb-4">Envie-nos uma Mensagem</h3>
                        
                        <!-- Alerta de Sucesso/Erro -->
                        <div id="formAlert" class="alert d-none" role="alert"></div>
                        
                        <!-- Formulário -->
                        <form id="contactForm" class="contact-form">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="nome" class="form-label">Nome Completo *</label>
                                    <input type="text" class="form-control" id="nome" name="nome" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="email" class="form-label">Email *</label>
                                    <input type="email" class="form-control" id="email" name="email" required>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="telefone" class="form-label">Telefone</label>
                                    <input type="tel" class="form-control" id="telefone" name="telefone">
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="assunto" class="form-label">Assunto *</label>
                                    <select class="form-select" id="assunto" name="assunto" required>
                                        <option value="">Seleccione o assunto</option>
                                        <option value="servicos">Solicitação de Serviços</option>
                                        <option value="informacao">Pedido de Informação</option>
                                        <option value="reclamacao">Reclamação</option>
                                        <option value="sugestao">Sugestão</option>
                                        <option value="outro">Outro</option>
                                    </select>
                                </div>
                            </div>
                            <div class="mb-3">
                                <label for="mensagem" class="form-label">Mensagem *</label>
                                <textarea class="form-control" id="mensagem" name="mensagem" rows="6" required placeholder="Descreva a sua mensagem em detalhe..."></textarea>
                            </div>
                            <div class="mb-3 form-check">
                                <input type="checkbox" class="form-check-input" id="newsletter" name="newsletter">
                                <label class="form-check-label" for="newsletter">
                                    Desejo receber actualizações e notícias do IACM
                                </label>
                            </div>
                            <button type="submit" class="btn btn-hero w-100" id="submitBtn">
                                <i class="fas fa-paper-plane me-2"></i>Enviar Mensagem
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Informações de Contacto -->
<section class="contact-info-section py-5 bg-light">
    <div class="container">
        <div class="row">
            <div class="col-lg-4 mb-4" data-aos="fade-up">
                <div class="info-card text-center">
                    <div class="info-icon">
                        <i class="fas fa-map-marker-alt"></i>
                    </div>
                    <h5>Endereço</h5>
                    <p>Alameda do Aeroporto<br>Maputo Cidade, Moçambique</p>
                </div>
            </div>
            <div class="col-lg-4 mb-4" data-aos="fade-up" data-aos-delay="100">
                <div class="info-card text-center">
                    <div class="info-icon">
                        <i class="fas fa-phone"></i>
                    </div>
                    <h5>Telefones</h5>
                    <p>Geral: (+258) 21468900<br>Emergência: (+258) 84 123 4567</p>
                </div>
            </div>
            <div class="col-lg-4 mb-4" data-aos="fade-up" data-aos-delay="200">
                <div class="info-card text-center">
                    <div class="info-icon">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <h5>Email</h5>
                    <p>geral@iacm.gov.mz<br>info@iacm.gov.mz</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Mapa -->
<section class="map-section">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="map-container">
                    <iframe 
                        src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3587.897646582656!2d32.57131231502907!3d-25.920783783548!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x1ee69b0f1c0e6c3b%3A0x5c5b5c5c5c5c5c5c!2sAlameda%20do%20Aeroporto%2C%20Maputo%2C%20Mozambique!5e0!3m2!1sen!2s!4v1234567890123!5m2!1sen!2s" 
                        width="100%" 
                        height="450" 
                        style="border:0;" 
                        allowfullscreen="" 
                        loading="lazy" 
                        referrerpolicy="no-referrer-when-downgrade">
                    </iframe>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Modal: Todos os Colaboradores -->
<div class="modal fade" id="staffModal" tabindex="-1" aria-labelledby="staffModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="staffModalLabel">
                    <i class="fas fa-users me-2"></i>Colaboradores IACM
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-4" id="staffContent">
                <!-- Loader -->
                <div class="text-center py-5" id="staffLoader">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Carregando...</span>
                    </div>
                    <p class="mt-3 text-muted">Carregando colaboradores...</p>
                </div>
                
                <!-- Conteúdo dos colaboradores (será preenchido via JavaScript) -->
                <div id="staffList" style="display: none;"></div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times me-1"></i>Fechar
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Scripts -->
<script>
// =============================================
// SCRIPT 1: ENVIO DO FORMULÁRIO DE CONTACTO
// =============================================
document.getElementById('contactForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    
    const submitBtn = document.getElementById('submitBtn');
    const formAlert = document.getElementById('formAlert');
    const form = e.target;
    
    // Desabilitar botão e mostrar loading
    submitBtn.disabled = true;
    submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Enviando...';
    
    // Esconder alertas anteriores
    formAlert.classList.add('d-none');
    
    try {
        // Coletar dados do formulário
        const formData = new FormData(form);
        const data = {
            nome: formData.get('nome'),
            email: formData.get('email'),
            telefone: formData.get('telefone'),
            assunto: formData.get('assunto'),
            mensagem: formData.get('mensagem'),
            newsletter: formData.get('newsletter') ? true : false
        };
        
        console.log('📤 Enviando dados:', data);
        
        // Enviar via AJAX para /contact (rota pública do routes/contact.js)
        const response = await fetch('/contact', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(data)
        });
        
        const result = await response.json();
        
        if (result.success) {
            // Sucesso
            formAlert.className = 'alert alert-success';
            formAlert.innerHTML = '<i class="fas fa-check-circle me-2"></i>' + result.message;
            formAlert.classList.remove('d-none');
            
            // Limpar formulário
            form.reset();
            
            // Scroll para o alerta
            formAlert.scrollIntoView({ behavior: 'smooth', block: 'center' });
            
        } else {
            // Erro
            formAlert.className = 'alert alert-danger';
            formAlert.innerHTML = '<i class="fas fa-exclamation-triangle me-2"></i>' + result.message;
            formAlert.classList.remove('d-none');
        }
        
    } catch (error) {
        console.error('❌ Erro ao enviar:', error);
        formAlert.className = 'alert alert-danger';
        formAlert.innerHTML = '<i class="fas fa-exclamation-triangle me-2"></i>Erro ao enviar mensagem. Por favor, tente novamente.';
        formAlert.classList.remove('d-none');
    } finally {
        // Reabilitar botão
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<i class="fas fa-paper-plane me-2"></i>Enviar Mensagem';
    }
});

// =============================================
// SCRIPT 2: CARREGAR COLABORADORES NO MODAL
// =============================================
document.getElementById('staffModal').addEventListener('show.bs.modal', async function () {
    try {
        const response = await fetch('/admin/staff/api/all');
        const data = await response.json();
        
        if (data.success) {
            const staffByDepartment = data.data;
            let html = '';
            
            // Iterar pelos departamentos
            Object.keys(staffByDepartment).forEach(department => {
                const members = staffByDepartment[department];
                
                html += `
                    <div class="mb-4">
                        <h4 class="text-primary border-bottom pb-2 mb-3">
                            <i class="fas fa-building me-2"></i>${department}
                        </h4>
                        <div class="row">
                `;
                
                // Iterar pelos membros do departamento
                members.forEach(member => {
                    html += `
                        <div class="col-md-6 col-lg-4 mb-3">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body text-center">
                                    ${member.photo ? 
                                        `<img src="${member.photo}" 
                                             alt="${member.name}" 
                                             class="rounded-circle mb-3"
                                             style="width: 100px; height: 100px; object-fit: cover;">` 
                                        : 
                                        `<div class="bg-secondary rounded-circle mx-auto mb-3 d-flex align-items-center justify-content-center text-white"
                                              style="width: 100px; height: 100px;">
                                            <i class="fas fa-user fa-3x"></i>
                                         </div>`
                                    }
                                    <h5 class="card-title mb-1">${member.name}</h5>
                                    <p class="text-muted small mb-2">${member.position}</p>
                                    <hr>
                                    <div class="text-start">
                                        <p class="mb-1 small">
                                            <i class="fas fa-phone text-primary me-2"></i>
                                            <a href="tel:${member.phone}" class="text-decoration-none">${member.phone}</a>
                                        </p>
                                        ${member.email ? 
                                            `<p class="mb-0 small">
                                                <i class="fas fa-envelope text-primary me-2"></i>
                                                <a href="mailto:${member.email}" class="text-decoration-none">${member.email}</a>
                                            </p>` 
                                            : ''
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    `;
                });
                
                html += `
                        </div>
                    </div>
                `;
            });
            
            // Exibir conteúdo
            document.getElementById('staffLoader').style.display = 'none';
            document.getElementById('staffList').innerHTML = html;
            document.getElementById('staffList').style.display = 'block';
        } else {
            throw new Error('Erro ao carregar dados');
        }
    } catch (error) {
        console.error('Erro ao carregar colaboradores:', error);
        document.getElementById('staffLoader').innerHTML = `
            <div class="text-center py-5">
                <i class="fas fa-exclamation-triangle fa-3x text-danger mb-3"></i>
                <h5 class="text-danger">Erro ao carregar colaboradores</h5>
                <p class="text-muted">Por favor, tente novamente mais tarde.</p>
            </div>
        `;
    }
});

// Resetar modal ao fechar
document.getElementById('staffModal').addEventListener('hidden.bs.modal', function () {
    document.getElementById('staffLoader').style.display = 'block';
    document.getElementById('staffList').style.display = 'none';
    document.getElementById('staffList').innerHTML = '';
});
</script>